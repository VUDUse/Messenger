<Page x:Class="Messenger.ChatPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Messenger"
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="600"
      Title="ChatPage">

    <Grid>
        <ScrollViewer x:Name="ChatScrollViewer" Margin="40,40,40,0" VerticalAlignment="Top" Height="350" VerticalScrollBarVisibility="Hidden" Background="#FF27232A" BorderBrush="#FF27232A">
            <StackPanel x:Name="ChatStackPanel" VerticalAlignment="Bottom">
                <!-- Сообщения будут добавляться сюда -->
            </StackPanel>
        </ScrollViewer>
        <TextBox x:Name="Message" Margin="40,0,92,40" TextWrapping="Wrap" Height="50" FontSize="20" VerticalAlignment="Bottom" KeyDown="Message_KeyDown" TextChanged="Message_TextChanged"/>
        <Button Content="Enter" HorizontalAlignment="Left" Margin="513,415,0,0" VerticalAlignment="Top" Click="Enter_Click"/>
    </Grid>
</Page>
    
    
