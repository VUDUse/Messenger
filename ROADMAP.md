# План развития проекта "Messenger"

---

## Фаза 0 — Безопасность и гигиена репозитория (делать первой, без исключений)

- [x] `chore`: добавить `.gitignore` (bin/obj, appsettings.*.json с секретами, .vs)
- [x] `refactor`: вынести строку подключения к SQL Server из `ApplicationContext.cs` в `appsettings.json` / переменные окружения
- [x] `feat`: хеширование паролей (BCrypt.Net-Next) для `Users.Password`; при логине сравнивать хеш, не строку
- [x] `feat`: Убрать пароль у чатов вовсе — см. Фазу 2
- [x] `refactor`: заменить `RoomID`/`Users.ID` со случайных строк на нормальные PK (`int identity` или `Guid`), подключить EF Core Migrations вместо `Database.EnsureCreated()`

---

## Фаза 1 — Полноценная регистрация, подтверждение почты и профиль (Ник#цифры)

Новая модель `Users`: `Email` (уникальный, `EmailConfirmed: bool`), `Nickname`, `Discriminator` (4 цифры), `PasswordHash`.
Публичный ID пользователя — строка вида `Ник#1234`, но в переписке отображается только `Nickname` (без `#1234`).
Инфраструктура отправки почты из этой фазы позже переиспользуется для 2FA и сброса пароля (Фаза 6).

- [ ] `refactor`: обновить сущность `Users` — добавить `Email`, `EmailConfirmed`, `Discriminator`
- [ ] `feat`: генерация уникального `Discriminator` при регистрации — при совпадении `Nickname` подбирать свободные 4 цифры (уникальность по паре `Nickname+Discriminator`)
- [ ] `feat`: сервис отправки почты (MailKit + SMTP); SMTP-креды хранить в конфиге, а не в коде (по аналогии с Фазой 0)
- [ ] `feat`: генерация кода подтверждения (6 цифр, TTL ~10 минут), сохранение кода во временной таблице/поле, привязанном к email
- [ ] `feat`: экран регистрации — поля Email, Nickname, Password; после сабмита открывается окно "Введите код из почты"
- [ ] `feat`: экран подтверждения кода — проверка кода, установка `EmailConfirmed = true`, кнопка "отправить код повторно" (с ограничением частоты)
- [ ] `refactor`: вход в аккаунт — по `Login + Password`
- [ ] `feat`: смена ника в Settings — меняется только `Nickname`, `Discriminator` остаётся неизменным (это и есть постоянный идентификатор профиля)
- [ ] `refactor`: во всех местах, где отображается отправитель сообщения (чат, список участников) — показывать только `Nickname`; полный `Ник#цифры` показывать только в профиле пользователя

**Важно:** пока `EmailConfirmed = false`, пользователю нельзя логиниться — учётку можно создавать сразу, но входить разрешать только после подтверждения кода

---

## Фаза 2 — Новая модель данных: личные чаты и группы

- [ ] `feat`: сущности `Conversations` (Type: Direct/Group), `ConversationMembers` (UserId, ConversationId, Role), `Messages` (Id, ConversationId, SenderId, Content, Type, Timestamp, IsEdited, IsDeleted)
- [ ] `refactor`: убрать логин/пароль у чат-комнаты — вступление в личный чат по `Nickname#Discriminator` собеседника, в группу — по инвайту/добавлению участника
- [ ] `refactor`: сервисный слой (`ChatService`, `UserService`) — вынести обращения к `DbContext` из code-behind страниц
- [ ] `feat`: перевести отправку/чтение сообщений на новую таблицу `Messages` вместо строки `ChatHistory`
- [ ] `feat`: UI создания группы (список участников, название, аватар)

---

## Фаза 3 — Вложения, эмодзи, аудио- и видеосообщения (хранение — BLOB в БД)

- [ ] `feat`: добавить в `Messages` поля `AttachmentData (byte[])`, `AttachmentFileName`, `AttachmentMimeType`, `AttachmentSize`
- [ ] `feat`: отправка сообщения с прикреплённым файлом и текстом одновременно (caption): файл+текст вместе, только текст, или только файл, любая комбинация
- [ ] `feat`: превью прикреплённого файла в поле ввода перед отправкой, с возможностью убрать вложение до отправки
- [ ] `feat`: отображение файла в чате (иконка/имя + кнопка "скачать" → сохранить byte[] на диск)
- [ ] `feat`: пикер эмодзи в поле ввода сообщения (Unicode-эмодзи через системный шрифт Segoe UI Emoji)
- [ ] `feat`: запись аудио через NAudio, отправка как voice-message
- [ ] `feat`: запись видео с камеры, отправка видео-сообщения
- [ ] `chore`: ограничение на размер вложения

---

## Фаза 4 — Шифрование сообщений

- [ ] `feat`: генерация пары ключей (публичный/приватный) для каждого пользователя при регистрации; приватный ключ хранится локально и зашифрован через DPAPI, публичный — в БД
- [ ] `feat`: End-to-End шифрование текста в личных чатах — сообщение шифруется публичным ключом собеседника перед отправкой, сервер хранит только шифротекст
- [ ] `feat`: расшифровка сообщений на стороне получателя приватным ключом при отображении
- [ ] `feat`: для групповых чатов — шифрование общим ключом группы на уровне сервера
- [ ] `chore`: задокументировать ограничения схемы (в README/комментариях к коду): нет forward secrecy и ротации ключей, нет поддержки нескольких устройств одного пользователя; при потере приватного ключа (переустановка ОС и т.п.) история личных сообщений становится нечитаемой — резервного копирования ключа в этой версии нет

---

## Фаза 5 — Realtime через SignalR (главный архитектурный шаг)

- [ ] `feat`: новый проект `Messenger.Server` (ASP.NET Core) — здесь будет жить SignalR Hub и доступ к БД
- [ ] `feat`: `ChatHub` — методы `SendMessage`, события `ReceiveMessage`, `UserTyping`
- [ ] `feat`: клиент (WPF) подключается к хабу при входе, подписывается на события
- [ ] `refactor`: отправка сообщения теперь идёт через хаб (`hub.SendMessage(...)`), а не прямой `db.SaveChanges()` из клиента
- [ ] `refactor`: убрать `DispatcherTimer`-поллинг чата — обновление UI по событию из хаба
- [ ] `feat`: индикатор "печатает…"

---

## Фаза 6 — Безопасность входа: лимиты, 2FA, сброс пароля

- [ ] `refactor`: полностью убрать `CheckIPAddressLogin`
- [ ] `refactor`: `AuthService` — хранить токен не в реестре, а в файле `%AppData%\Messenger\auth.dat`, шифруя через `System.Security.Cryptography.ProtectedData`
- [ ] `feat`: явный logout с инвалидацией токена на сервере
- [ ] `feat`: лимит попыток входа — счётчик неудачных попыток (`FailedLoginAttempts`) и временная блокировка аккаунта (`LockedUntil`) с нарастающей задержкой
- [ ] `feat`: 2FA по email — при входе после верного пароля отправляется код подтверждения на почту (переиспользует инфраструктуру Фазы 1); вход завершается только после ввода кода
- [ ] `feat`: сброс/смена пароля через email — экран "Забыли пароль?", код подтверждения на почту, форма ввода нового пароля
- [ ] `feat` (следующим шагом, после email-2FA): TOTP через приложение-аутентификатор (Google Authenticator и т.п., библиотека Otp.NET) как более надёжная альтернатива email-коду

---

## Фаза 7 — Аудиозвонки 1-на-1

Первая версия — только аудио, только между двумя пользователями. Видео и групповые звонки — отдельная задача.

- [ ] `chore`: сравнить варианты реализации WebRTC в .NET — библиотека SIPSorcery vs встраивание WebView2 с готовым JS WebRTC-стеком — и зафиксировать выбор

  **Заметка для этого решения:** WebView2 + JS использует WebRTC-движок браузера (Chromium), но добавляет в проект JS для одного экрана звонка. SIPSorcery — вариант на чистом C#.

- [ ] `feat`: сигнализация звонка через существующий SignalR Hub (приглашение на звонок, принятие/отклонение, обмен SDP/ICE-кандидатами)
- [ ] `feat`: настройка STUN/TURN-сервера для обхода NAT (свой coturn либо облачный сервис)
- [ ] `feat`: захват и передача аудио с микрофона, воспроизведение у собеседника
- [ ] `feat`: UI звонка — входящий/исходящий вызов, кнопки принять/отклонить/завершить
- [ ] `chore`: явно ограничить scope первой версии в README/Issue — только аудио, только 1-на-1

---

## Фаза 8 — Экстренные сообщения и звонки

Отдельный тип сообщения/звонка с повышенным приоритетом — должен пробиваться даже при выключенных уведомлениях/звуке.

- [ ] `feat`: тип сообщения `IsEmergency` (`Messages`) и аналогичный флаг для звонка
- [ ] `feat`: экстренное сообщение/звонок игнорирует собственную настройку приложения "уведомления/звук выключены"
- [ ] `feat`: снятие mute с аудио-сессии приложения через Core Audio API (NAudio `SimpleAudioVolume`) — если пользователь замьютил именно приложение в Volume Mixer Windows, экстренный сигнал всё равно даст звук
- [ ] `feat`: принудительный вывод окна на передний план + мигание иконки в панели задач (`FlashWindowEx`) как запасной визуальный канал
- [ ] `feat`: экран/подтверждение перед отправкой экстренного сообщения или звонка (защита от случайного использования как обычного спама)
- [ ] `feat`: подсказка пользователю — добавить приложение в приоритетные уведомления Windows (Focus Assist), чтобы экстренные оповещения проходили и в режиме "Фокусировка внимания"
- [ ] `chore`: задокументировать честную границу возможностей — при полностью выключенном звуке устройства (нулевая громкость/аппаратный mute) в этом случае остаётся только визуальный канал (мигание/окно на передний план)

---

## Фаза 9 — Визуал и навигация (WPF-UI)

- [ ] `chore`: подключить пакет WPF-UI, изучить `NavigationView` и готовые стили
- [ ] `refactor`: перевести `SettingsMenu` и другие экраны, которые сейчас открываются как отдельный `Window`, в `Page` внутри единого `NavigationView` — один экземпляр окна на всё приложение вместо нескольких
- [ ] `feat`: анимированные переходы между страницами (fade/slide) через встроенные возможности WPF-UI вместо мгновенной смены экрана
- [ ] `feat`: тёмная/светлая/системная тема через встроенный в WPF-UI переключатель (вместо самописных `Themes/Dark.xaml`/`Light.xaml`)
- [ ] `feat`: Settings как выезжающая панель поверх текущего чата, а не отдельный полноэкранный переход
- [ ] `feat`: унифицировать кастомную рамку окна (свои back/minimize/close, `DragMove`) в едином стиле по всему приложению вместо разнобоя между экранами

---

## Фаза 10 — Доп. функционал (по мере желания/времени)

- [ ] `feat`: статусы сообщений (доставлено/прочитано)
- [ ] `feat`: редактирование и удаление своих сообщений
- [ ] `feat`: аватары пользователей и групп
- [ ] `feat`: поиск по истории сообщений
- [ ] `feat`: toast-уведомления о новых сообщениях
- [ ] `feat`: локализация RU/EN
- [ ] `chore`: логирование через Serilog вместо тихих падений try/catch
- [ ] `chore`: GitHub Actions workflow — сборка проекта на каждый push/PR
- [ ] `test`: базовые unit-тесты для сервисного слоя
